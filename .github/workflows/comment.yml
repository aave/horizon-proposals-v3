name: PR Comment

on:
  workflow_run:
    workflows: [Test]
    types:
      - completed

permissions:
  actions: read
  contents: write
  pull-requests: write

jobs:
  test:
    uses: aave-dao/github-workflows/.github/workflows/comment.yml@main
